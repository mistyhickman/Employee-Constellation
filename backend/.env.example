DATABASE_URL="postgresql://docme360:docme360@localhost:5432/docme360"
JWT_SECRET="dev-only-secret-change-me"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
# Leave unset (or "false") for local/plain-HTTP dev. See docs/09-deployment.md
# for the hosted-deployment env vars (POSTGRES_PASSWORD, PUBLIC_ORIGIN, etc.).
COOKIE_SECURE=false
